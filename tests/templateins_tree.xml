<templateins id="pkt_counter_aggregator_tree">
  <description>
    One node counts packets and sends the report to another node, 
    which prints the total number of packets being received.
  </description>

  <vars>
    <var name="bpf_filter" value="port 443"/>
	<var name="num_nodes" value="3" topology="yes"/>
  </vars>

</templateins>
